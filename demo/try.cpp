#include <iostream>
using namespace std;

int main() {
    // int n;
    // cout << "Enter the size of the array: ";
    // cin >> n;

    // int arr[n];
    // cout << "Enter the elements of the array: ";
    // for (int i = 0; i < n; i++) {
    //     cin >> arr[i];
    // }

    // Insertion Sort Algorithm
    // for (int i = 1; i < n; i++) {
    //     int temp = arr[i];
    //     int j = i - 1;

    //     for (; j >= 0; j--) {
    //         if (arr[j] > temp) {
    //             arr[j + 1] = arr[j];
    //         } else {
    //             break; 
    //         }
    //     }
    //     arr[j + 1] = temp; 
    // }

    // cout << "Sorted array: ";
    // for (int i = 0; i < n; i++) {
    //     cout << arr[i] << " ";
    // }
    // cout << endl;

    int a = 1 % 5;
    int b = 3 % 5;
    int c = 4 % 5;

    cout << "a: " << a << " "<< b << " "<< c << endl;

    return 0;
}
